apiVersion: v1
kind: Service
metadata:
  name: finops-dashboard
  namespace: kube-system
  labels:
    app.kubernetes.io/name: finops-dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: finops-dashboard
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
      name: http
