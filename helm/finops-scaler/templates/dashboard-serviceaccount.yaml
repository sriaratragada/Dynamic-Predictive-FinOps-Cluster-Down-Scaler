apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "finops-scaler.dashboard.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "finops-scaler.labels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard
